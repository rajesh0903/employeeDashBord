package com.rky.config;

public class GuavaCacheConfig {

	/*
	 * public CacheManager cacheManager(){ GuavaCacheManager cacheManager = new
	 * GuavaCacheManager();
	 * cacheManager.setCacheBuilder(CacheBuilder.newBuilder().expireAfterWrite(24,
	 * TimeUnit.HOURS)); cacheManager.setCacheNames(Arrays.asList("ticketsCache"));
	 * return cacheManager; }
	 */
	
	
}
